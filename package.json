{
  "name": "ai-quota-monitor",
  "version": "0.0.4",
  "private": true,
  "description": "AI quota monitor",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "start": "tauri dev",
    "start:tauri": "tauri dev",
    "test": "node --test \"test/**/*.test.js\"",
    "build:tauri": "tauri build",
    "build:zip": "npm run build:tauri && powershell -NoProfile -ExecutionPolicy Bypass -File scripts/build-zip.ps1"
  },
  "dependencies": {},
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  },
  "author": "Takahiro SUZUKI"
}
